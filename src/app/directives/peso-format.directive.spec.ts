import { PesoFormatDirective } from './peso-format.directive';

describe('PesoFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new PesoFormatDirective();
    expect(directive).toBeTruthy();
  });
});
